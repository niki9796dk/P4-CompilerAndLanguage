// Test of legal but ugly syntax.
block random_04 {
    channel:in input
	;
    channel:out output
	;
    
    blueprint() {
        block a = build test()											;
			block b = build test()									;
				block c = build test()							;
					block d = build test()					;
						block e = build test()			;
							block f = build test()	;
						block g = build test()			;
					block h = build test()					;
				block i = build test()							;
			block j = build test()									;
		this.input 
		
			->
		
             a
           ->  b 
         ->  c  -> 
       d -> e -> f ->
     g -> h  ->  i -> j
	 
			->
		this.output
			;
    }
}
