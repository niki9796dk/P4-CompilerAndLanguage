block procedure_01 {
    mychannel:in in;
    mychannel:out out;

    blueprint () {
        source fakeBlock = build Source();

        abc(fakeBlock);

        this.in -> this.out;
    }

    procedure abc(block b) {

    }
}