block procedure_01 {
    mychannel:in in;
    mychannel:out out;

    blueprint () {
        source fakeBlock = build Source([4,4]);

        abc(fakeBlock);

        this.in -> this.out;
    }

    procedure abc(block b) {

    }
}