block procedure_01 {
    mychannel:in in;
    mychannel:out out;

    blueprint () {
        source fakeBlock = build Source([1,1]);

        abc(fakeBlock);

        this.in -> this.out;
    }

    procedure abc(block b) {

    }
}