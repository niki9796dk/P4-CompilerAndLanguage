block procedure_03 {
    mychannel:in in;
    mychannel:out out;

    blueprint () {
        size fakeBlock = [25,20];

        abc(fakeBlock);

        this.in -> this.out;
    }

    procedure abc(block b) {

    }
}