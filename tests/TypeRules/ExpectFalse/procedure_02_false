block procedure_02 {
    mychannel:in in;
    mychannel:out out;

    blueprint () {
        operation fakeBlock = build _Sigmoid();

        abc(fakeBlock);

        this.in -> this.out;
    }

    procedure abc(block b) {

    }
}
